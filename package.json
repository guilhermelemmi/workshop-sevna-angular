{
  "name": "workshop-sevna-angular",
  "private": true,
  "version": "0.0.1",
  "description": "A sample AngularJS Application",
  "repository": "https://github.com/guilhermelemmi/workshop-sevna-angular",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "update-deps": "npm update",
    "postupdate-deps": "bower update",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  }
}
